<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.yungov.module.impl.system.dao.SysMenuMapper">

    <resultMap id="BaseResultMap" type="cn.com.zerobug.module.impl.system.domain.dataobject.SysMenuDO">
        <result column="id" property="id"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_delete" property="isDelete"/>
        <result column="name" property="name"/>
        <result column="permission" property="permission"/>
        <result column="menu_type" property="menuType"/>
        <result column="level" property="level"/>
        <result column="sort" property="sort"/>
        <result column="parent_id" property="parentId"/>
        <result column="path" property="path"/>
        <result column="icon" property="icon"/>
        <result column="component" property="component"/>
        <result column="is_link" property="isLink"/>
        <result column="is_show" property="isShow"/>
        <result column="is_cache" property="isCache"/>
        <result column="status" property="status"/>
        <result column="tenant_id" property="tenantId"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,name,permission,
        menu_type,level,sort,parent_id,
        path,icon,component,is_link,is_show,is_cache,
        status,create_by,create_time,
        update_by,update_time,is_delete,
        tenant_id
    </sql>

    <select id="selectPermissionByUserId" resultType="java.lang.String">
        SELECT permission
        FROM sys_menu m
                 LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
                 LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE m.is_delete = 0
          AND ur.user_id = #{userId}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT m.id,
               m.name,
               m.permission,
               m.menu_type,
               m.level,
               m.sort,
               m.parent_id,
               m.path,
               m.icon,
               m.component,
               m.is_link,
               m.is_show,
               m.is_cache,
               m.status,
               m.create_by,
               m.create_time,
               m.update_by,
               m.update_time,
               m.is_delete,
               m.tenant_id
        FROM sys_menu m
                 LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
                 LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE m.is_delete = 0
          AND ur.user_id = #{userId}
    </select>
</mapper>
